# AUTOMATON v2 - Environment examples
#
# Frontend/Vite reads environment files from frontend/. Copy the frontend
# example there when you need to override the default backend URL.
#
# Electron accepts FRONTEND_URL from the shell environment. It defaults to
# http://localhost:5173, so no variable is required for normal development.
FRONTEND_URL=http://localhost:5173
